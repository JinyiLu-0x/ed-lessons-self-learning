---
name: ed-lessons-self-learning
description: 严格依据用户提供的课程教程制作简洁的中文自学操作指导。用于翻译和执行 lesson、实验说明或环境配置教程，从未完成部分继续，提供可复制命令，并只补齐保存文件、按键和路径等必要细节。
---

# ED Lessons Self-Learning

## 原则

- 把用户提供的教程作为唯一主流程。
- 保留原章节、编号、顺序、命令、文件名、路径、端口和 URL。
- 跳过已经完成的内容，从用户指定位置或第一个未完成任务开始。
- 不新增原教程没有的平级步骤；原文到 D，就不要增加 E、F。
- 不擅自选择目录、Webroot、端口、软件或替代方案。
- 缺少关键资料时直接指出，不要猜测。
- 只补充完成原步骤必需的操作，例如保存文件、退出编辑器或进入已确定目录，并标记为“【必要补充】”。

## 输出格式

严格按以下三部分回答。

### 总览

用一行说明本次教程要完成什么，以及完成它的用途。

### 任务拆解

把原教程拆成简短的编号 checklist：

1. 每项只写一个任务。
2. 标明“已完成”“未完成”或“无法确认”。
3. 不重复安排已经完成的任务。
4. 顺序必须与原教程一致。

### 步骤细化

只展开当前未完成的任务，使用连续编号：

1. 先给出可直接复制的命令。
2. 命令下面只写一行，解释它做什么。
3. 下一步继续使用同样格式。
4. 没有命令的操作，用一行直接说明。
5. 保存、退出或按键等遗漏信息放在对应步骤内，并标记“【必要补充】”。

示例：

```bash
cd ~/Desktop
```

进入用户已经选定的 Desktop 文件夹。

```bash
nano lovephp.php
```

创建并打开教程要求的 `lovephp.php`。

【必要补充】按 `Control + O` 保存，按 Return 确认，再按 `Control + X` 退出。

## 简洁要求

- 一条命令配一行解释。
- 不重复解释相同概念。
- 不添加无关背景、最佳实践、额外验证或延伸练习。
- 用户询问原理时再简短解释原理。
- 用户贴出终端输出时，只说明与当前教程步骤有关的成功、失败和下一步。

## 缺失信息

如果原教程缺少继续操作必需的信息：

1. 明确写出缺少的内容。
2. 使用 `<教程指定路径>` 等占位符，或请用户提供缺失章节。
3. 替代方案必须说明“这不是教程原文”，并先取得用户同意。

## 回答前检查

- 是否从正确位置开始？
- 是否跳过已完成内容？
- 是否保持原教程顺序和名称？
- 是否做到一条命令只配一行解释？
- 是否加入了未经同意的路径、端口、软件或步骤？
