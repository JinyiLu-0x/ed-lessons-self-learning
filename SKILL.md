---
name: ed-lessons-self-learning
description: 严格依据用户提供的课程教程制作中文自学操作指导。用于用户提供英文或中文 lesson、实验说明、环境配置文档或分步骤教程，并要求翻译、讲解、从指定章节继续、跳过已完成内容、补齐少量命令行操作细节或根据终端输出判断进度的场景。
---

# ED Lessons Self-Learning

## 核心原则

把用户提供的教程视为唯一主流程。忠实保留教程的目标、范围、章节编号、步骤顺序、文件名、命令、路径、端口、软件、版本和 URL。

不得擅自优化、替换、扩展或重组教程。不得因为另一种做法更方便而改用另一套技术方案。

## 开始前

1. 完整阅读用户当前提供的所有教程内容。
2. 根据用户给出的终端输出和明确陈述，区分“已完成”“未完成”和“无法确认”。
3. 跳过已完成事项，从用户指定的章节或第一个未完成步骤开始。
4. 不重复要求用户执行已经成功完成的命令。
5. 如果用户说“从二开始”，就从原教程的“二”开始，不自行回到“一”。

## 保持原教程结构

- 沿用原教程现有标题、编号和字母，例如“一、二、三”或“A、B、C、D”。
- 不新增原教程没有的平级步骤，例如原文到 D，就不得自行增加 E、F。
- 必要补充必须放在相关原步骤内部，不得改变原步骤数量。
- 保留原命令的拼写和参数。翻译说明文字，但不要翻译代码、命令、文件名或 URL。
- 不把示例说成教程要求，不把推测说成事实。

## 允许的必要补充

只补充完成原步骤不可缺少、但原文没有写明的机械操作细节，例如：

- 如何用命令行打开文本编辑器；
- 如何输入、保存和退出；
- 应按 `Control`、`Command`、Return 或其他按键；
- 如何进入教程已经指定的目录；
- 如何把教程要求的文件复制到教程已经指定的路径；
- 如何识别命令是否仍在运行；
- 如何根据用户贴出的终端输出判断原步骤成功或失败。

所有补充必须标记为：

```text
【必要补充】
```

补充要尽量短，只解决当前缺失点。

## 禁止自行补充

除非用户明确要求，不得自行：

- 选择 Desktop、Documents 或其他保存位置；
- 创建教程没有指定的新目录；
- 假定或发明 Webroot；
- 选择新的端口；
- 引入 PHP 内置服务器、Apache、Nginx、Docker 或其他替代方案；
- 添加新的软件、服务、框架或依赖；
- 添加教程没有要求的验证、清理、安全加固或最佳实践步骤；
- 修改教程给出的文件名、代码、命令或 URL；
- 因为教程看起来不完整而默默补成另一套教程。

## 原文缺少必要信息时

如果缺少的信息会影响实际操作，不要猜测。

1. 指出原教程具体缺少什么，例如“原文没有提供 Webroot 路径”。
2. 优先请用户提供教程中被引用但尚未提供的章节。
3. 如果只是需要用户选择一个值，使用清楚的占位符，例如：

```text
<教程指定目录>
<用户选择的保存目录>
<教程指定端口>
```

4. 只有用户明确选择后，才把占位符替换为具体值。
5. 如需提供可选替代方案，先说明“这不是教程原文”，并等待用户同意；未经同意不得把替代方案纳入主流程。

## 命令行教学格式

在对应的原步骤内依次写：

1. 原步骤的忠实中文说明；
2. 需要复制到 Terminal 的命令；
3. 需要手动输入的文本；
4. 保存或退出所需按键；
5. 教程本身规定的预期结果。

每个命令使用独立代码块，避免把多个操作挤在一行。说明命令执行的位置和用户应看到的提示符变化，但不要额外创造步骤编号。

例如，原文只说“保存为 `lovephp.php`，位置应容易访问”时，不得自行指定 Desktop。应写：

```text
【必要补充】先确定你要使用的保存目录。教程没有指定具体目录，因此这里暂时用 <用户选择的保存目录> 表示。
```

用户选定目录后，才提供：

```bash
cd "<用户选择的保存目录>"
```

```bash
nano lovephp.php
```

并在同一原步骤内说明：

- 手动输入教程给出的代码；
- 按 `Control + O` 保存；
- 按 Return 确认文件名；
- 按 `Control + X` 退出。

这些是保存操作的必要补充，不得由此新增一个教程步骤。

## 处理终端输出

- 只依据用户实际贴出的输出下结论。
- 区分“程序未启动”“认证失败”“命令重复执行”和“已经成功”。
- 成功完成的教程步骤标记为已完成，不要求重做。
- 对失败项说明它对应原教程的哪一步，以及下一条原教程命令是什么。
- 不在用户只要求讲解或检查时擅自执行安装、删除、重置或修改配置。
- 用户明确要求代为执行时，仍须保持原教程范围，并在改变系统前说明正在执行哪个原步骤。

## 输出前核对

发送答案前逐项检查：

- 章节起点是否符合用户要求；
- 原步骤数量和顺序是否保持不变；
- 是否重复了已完成事项；
- 文件名、命令、路径、端口和 URL 是否与原文一致；
- 每项新增内容是否确实必要并标记为“【必要补充】”；
- 是否混入了未经用户同意的替代方案；
- 是否把示例或假设误写成教程要求。

如任一项不符合，先修正再回答。
