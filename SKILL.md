---
name: ed-lessons-self-learning
description: 严格依据用户提供的任意课程材料制作中文自学指导，适用于所有课程和任意工作文件夹中的 ED lesson、lesson、lab、tutorial、workshop、实验说明、环境配置教程、Week Overview、课程通知、题目与答案、多段视频、视频转写、lecture slides、slide deck、PPT 或分段说明文字。自动区分操作教程、多段视频、slides、题目知识点、问答题协作写作与一般文字说明；教程型从未完成部分继续并提供可复制命令；概念、预习、workshop、slides 和一般文字默认整理成“核心版学习笔记”，用编号知识点、bullet points、例子和“最该记住的句子”帮助复习；题目与答案提炼为去重、可直接复制的简洁知识点笔记，并保留关键英语术语；问答题协作写作先依据题目和知识点给中文作答大纲，再根据用户想法进行一轮诊断与修订，用户同意后输出英文版本。
---

# ED Lessons Self-Learning

这是一个全课程通用 skill；不要把它限定为某一门课。只要用户要求学习、讲解、跟做 ED、lesson、lab、tutorial、workshop、题目与答案、问答题协作写作、多段视频或 lecture slides，都按本 skill 执行。

## 总原则

- 把用户提供的课程材料作为唯一主流程。
- 保留原章节、编号、顺序、命令、文件名、路径、端口、URL 和关键英语术语。
- 不擅自选择目录、Webroot、端口、软件或替代方案。
- 缺少关键资料时直接指出，不要猜测。
- 只补充完成原步骤必需的操作，例如保存文件、退出编辑器或进入已确定目录，并标记为“【必要补充】”。
- 对 Week Overview、课程通知、概念说明、课堂安排等纯文字材料，默认整理成“核心版学习笔记”，不强行改成命令步骤。
- 讲解保留关键英语术语，例如 `Workshop`、`Applied Session`、`IDE`、`compile`、`execute`、`object-oriented`、`classes`、`objects`。
- 如果 ED、lab、tutorial 或 workshop 材料很长，并且原文包含 `Task 1`、`Task 2`、`Task 3.1` 等任务编号，输出时必须显式标清楚对应的 Task 编号与标题。
- 不把其他对话、之前完成的操作或外部知识混入当前材料，除非用户明确要求关联。
- 忽略明显不属于课程材料的界面残留，例如审批文字、模型名称、按钮标签和聊天控制信息。
- 材料被截断、听不清或缺页时直接标注，不能把不完整内容说成“完整总览”。

## 模式判断

先判断用户提供的是哪一类材料。

### A. 教程型材料

符合以下任意特征时，使用“教程型输出”：

- 有明确操作步骤、命令、路径、文件名、按钮、安装流程或代码执行要求。
- 用户要求“从这里开始做”“下一步是什么”“检查 Terminal 输出”“安装/配置/运行/提交”。
- 材料包含 lesson、lab、activity 中需要实际完成的操作。

### B. 多段视频材料

符合以下任意特征时，使用“多段视频输出”：

- 用户一次提供多个独立视频、视频转写、视频摘要或按视频分段的内容。
- 用户要求融合整理多段视频，同时仍保留每个视频的边界和顺序。

### C. Slides 材料

符合以下任意特征时，使用“Slides 输出”：

- 用户提供 lecture slides、slide deck、PPT、PowerPoint、导出的 slides 文字或 slides 截图。
- 内容明显由连续页面、页面标题、图表或 lecture deck 章节构成。
- 用户要求“这份 slides 在说什么”“整理这套 slides”“按 slides 自学”。

### D. 题目与答案材料

符合以下任意特征时，使用“题目知识点输出”：

- 用户同时提供题目与答案，并要求整理、提炼或制作学习笔记。
- 用户一次提供一道或多道 quiz、练习题、选择题、判断题、简答题或代码题及其答案。
- 用户重点需要题目背后的概念、术语、规则或代码知识点，而不是逐题复述。

### E. 一般文字说明材料

符合以下任意特征时，使用“文字说明型输出”：

- 主要是 Week Overview、课程通知、学习目标、课堂安排、assessment 说明或背景介绍。
- 是单段视频、单段转写、分段课程说明文字，且不属于完整 slide deck。
- 没有要求立即执行的命令或具体软件操作。
- 用户要求“讲解一下”“用中文说一遍”“突出重点”“帮我理解这段”。

### F. 问答题协作写作材料

符合以下任意特征时，使用“问答题协作写作输出”：

- 用户提供问答题、short-answer question、essay question、case question、reflection question 或 assessment question，并要求“根据知识点一起完成”“写题的大纲”“帮我组织答案”“我先给想法你帮我改”。
- 用户明确要求先给作答框架，再让用户补充想法，然后进行一轮诊断与修订。
- 用户要求最终同时输出中文大纲和英文版本。

如果用户只提供题目但没有知识点，先要求用户提供课程材料或知识点；不要用外部知识代替课程材料。若题目本身包含足够知识点，可明确说明“以下仅依据题目文本中可见信息”。

如果材料同时包含说明和操作，先讲清说明重点，再按原顺序列出操作步骤。不要仅因 slides 中出现软件或命令，就把整套 slides 误判为教程。

## 核心版学习笔记输出格式

当材料属于 Week Overview、课程通知、概念说明、pre-reading、workshop 核心内容、单段视频、单段转写、slides 概念内容或用户要求“讲解 / 核心版 / 复习 / 自学”时，默认使用本格式。除非用户明确要求摘要，否则不要输出只有三段的粗略概括。

```markdown
# 课程 / Week / 原标题 核心版

## 1. 小节主题
`核心公式 / 核心句 / English term = 简短解释`

用 1 至 3 句话解释这个知识点是什么、为什么重要，以及它在课程或作业中的用途。

核心关键词：

- `term 1`：中文解释。
- `term 2`：中文解释。
- `term 3`：中文解释。

区别 / 规则 / 例子：

- 按原材料顺序列出关键区别、规则、示例、教师提醒或 assessment 连接。
- 每条 bullet 只讲一个点。
- 如果原文给了强弱例子、表格、链条或对比，必须保留并转成易复习的 bullet points。

## 2. 下一个小节主题
`核心公式 / 核心句`

继续按原材料真实顺序整理。

## 最该记住的 8-12 句话

- 用可背诵、可复习的短句总结。
- 保留关键 English terms。
```

具体规则：

1. 生成任何核心版学习笔记时，必须按内容层级使用 Markdown heading markers，方便 Obsidian 生成目录；不要只在用户要求“处理标题”时才加井号。
2. 核心版最外层标题使用一级 Markdown 标题 `#`，例如 `# FIT2002 Week 2 Ed 核心版`。
3. 每个主要概念使用二级 Markdown 标题 `##` 并单独编号，例如 `## 1. Project Proposal`、`## 2. Output / Outcome / Benefit / Value`；不要只用加粗文本当章节标题。
4. 如果主要概念内部还需要分层，例如 `Functional requirement` 与 `Non-functional requirement`、`Project Proposal` 与 `Business Case` 的内部拆解，可使用三级 Markdown 标题 `###`；不要为了装饰滥用更深层级。
5. 结尾的“最该记住的 8-12 句话”也使用二级 Markdown 标题 `##`。
6. 每个编号小节开头尽量在标题下一行给一个反引号核心句，例如 `Output is what you build; value is why it matters`；核心句不要和标题挤在同一行。
7. 小节内部必须使用 bullet points，不要把多个要点写成一整块长段落。
8. 优先拆出 `核心关键词`、`区别`、`例子`、`常见错误`、`作业连接`、`为什么重要`，但只使用原材料支持的内容。
9. 原材料中的表格、链条、强弱例子和 assessment 提醒不能压缩丢失；要改写成便于复习的 bullet points。
10. 对容易混淆的概念必须并列对比，例如 `Product scope vs Project scope`、`Assumption vs Risk`、`Proposal vs Charter vs Scope Statement`。
11. 输出要比普通摘要更细，但不要逐句翻译；目标是能直接作为 Obsidian 复习笔记。
12. 末尾必须加入“最该记住的 8-12 句话”；材料很短时可减少到 3-6 句。
13. 如果用户要求“只处理标题 / 加井号 / 方便 Obsidian 目录”，只修改标题行的 Markdown heading marker，并按层级使用 `#`、`##`、`###`；正文、bullet points、颜色标签、`<font>`、`<mark>`、加粗、代码反引号和用户已有内容一律保持原样。
14. 清理旧输出时，形如 `**1. Topic**`、`**2. Topic**  ` 的主要编号标题应改为 `## 1. Topic`、`## 2. Topic`；如果原文已有 `#` 或 `##`，按现有层级保持一致，不重复添加井号，并保留标题与下一行核心句之间的换行。

## 教程型输出格式

严格按以下三部分回答。

### 总览

用一行说明本次教程要完成什么，以及完成它的用途。

### 任务拆解

把原教程拆成简短的编号 checklist：

1. 每项只写一个任务。
2. 标明“已完成”“未完成”或“无法确认”。
3. 不重复安排已经完成的任务。
4. 顺序必须与原教程一致。

### 步骤细化

只展开当前未完成的任务，使用连续编号：

1. 先给出可直接复制的命令。
2. 命令下面只写一行，解释它做什么。
3. 下一步继续使用同样格式。
4. 没有命令的操作，用一行直接说明。
5. 保存、退出或按键等遗漏信息放在对应步骤内，并标记为“【必要补充】”。

示例：

```bash
cd <教程指定目录>
```

进入原教程或用户已经明确指定的目录。

```bash
nano <教程指定文件>
```

创建并打开原教程要求的文件。

【必要补充】按 `Control + O` 保存，按 Return 确认，再按 `Control + X` 退出。

## 多段视频输出格式

每段视频分别整理为一个 Part，并按原顺序编号。相似内容出现在不同视频时，仍分别放在对应 Part，不擅自合并。

```markdown
# Part 1：视频主题

## 1. 这段视频在说什么

用 2 至 4 句话概括这一段视频的核心内容。

## 1.1 小节主题

- 是什么意思：……
- 为什么重要：……
- 教师提醒／规则／示例：……

## 1.2 小节主题

按照视频顺序继续整理。

## Part 1 小结

- 用 3 至 6 条短句总结最需要记住的内容。
```

具体规则：

1. 根据视频中的真实主题变化决定是否拆成 `1.1`、`1.2`、`1.3`；内容不足时不要强行拆分。
2. Part 2 使用 `2.1`、`2.2`，Part 3 使用 `3.1`、`3.2`，依次类推。
3. 严格遵循视频实际内容和讲解顺序，不逐字翻译。
4. 不能遗漏重要定义、规则、示例、操作步骤、教师提醒和容易混淆的区别。
5. 如果出现操作步骤，按原顺序列出；不要把整段视频改造成教程 checklist。
6. 听不清或无法确认的内容直接标注“【无法确认】”，不能猜测。

## Slides 输出格式

把一份 slide deck 作为一个整体，按 slides 中真实的主题变化分节。优先使用“核心版学习笔记输出格式”；不要逐页机械翻译，也不要把整套 slides 平铺成没有层级的长文。

```markdown
# Week / Lecture：Slides 原标题

## 1. 这份 slides 在说什么

用 2 至 4 句话说明这套 slides 的核心主题、内容范围和学习目标。

## 1.1 小节主题（保留关键 English term）

- 定义／是什么：……
- 为什么重要：……
- 规则／示例／教师提醒：……

## 1.2 小节主题

按照 slides 顺序继续整理。

## Slides 小结

- 用 3 至 6 条短句总结最需要记住的内容。
```

具体规则：

1. 优先保留 slides 原有的 Week、lecture 标题、章节名和页面顺序。
2. 连续多页属于同一主题时合并为一个小节；主题真正变化时再增加 `1.2`、`1.3`，不要强制一页一个小节。
3. 每个小节解释“是什么意思”和“为什么重要”；按内容需要补充定义、组成、流程、优缺点、例子、教师提醒或操作步骤。
4. 课程人员名单和普通欢迎语可以压缩；assessment、出勤、提交、软件要求、AI 规则和教师明确提醒不能遗漏。
5. 技术概念、流程图、架构图和对比关系需要展开说明；无法看到关键图片时使用 `【图片提醒】`，不要猜图。
6. 只讲当前 slides 中出现的内容，不引用之前对话中的命令、端口、文件、schema 或用户已完成操作。
7. 如果只拿到 slides 的部分文字或内容在中途截断，明确写“当前材料不完整”，不要声称是完整 lecture 总览。

## 题目知识点输出格式

将题目与答案提炼成简洁、可直接复制到学习笔记中的知识点。不要逐题复述题干和答案；一次提供多道题时，合并重复概念，只保留不重复的核心知识点。

优先使用：

```markdown
**English term（中文解释）**
`code example`：一句话说明含义、用途或规则。
```

没有必要的代码示例时，直接用一行解释：

```markdown
**primitive types（基本数据类型）**
Java 内置的基础类型，直接存储简单值，例如 `int`、`double`、`char` 和 `boolean`。
```

对容易混淆的概念使用并列对比：

```markdown
**variable declaration（变量声明）**
`double airfare;`：声明变量及其类型，但没有赋值。

**variable definition / initialisation（变量定义并初始化）**
`double airfare = 500.0;`：声明变量并同时赋予初始值。
```

具体规则：

1. 每个知识点尽量用一行讲清楚；只有并列对比或必要代码说明才使用第二行。
2. 保留重要 English terms，并在第一次出现时补充简短、准确的中文解释。
3. 不逐字翻译全部英文，只解释会影响理解的专业术语。
4. 代码示例必须短小，只保留理解该知识点所需的部分。
5. 不添加题目和答案没有支持的背景、扩展知识、总结段落或重复解释。
6. 多题涉及同一概念时只整理一次；不同概念按理解逻辑排列，不按题号重复展开。
7. 使用自然、准确的术语翻译：`primitive type` 为“基本数据类型”，`literal` 为“字面量”，`declaration` 为“声明”，`initialisation` 为“初始化”。
8. 如果答案疑似错误或与题目矛盾，标记“【答案待确认】”，不要把冲突内容写成确定知识点。

## 问答题协作写作输出格式

当用户要一起完成问答题、short-answer question、essay question、case question、reflection question 或 assessment question 时，按“先搭骨架，再让用户参与，一轮诊断修订，用户同意后再出英文稿”的流程执行。目标不是直接代写完整答案，而是帮助用户把课程知识点组织成可写、可改、可提交前继续扩展的大纲。

### 第 0 步：确认材料

先检查是否具备以下信息：

- 题目原文或截图中可读出的题目。
- 课程知识点、slides、lesson notes、rubric、marking criteria 或老师提示。
- 字数、格式、是否需要引用、是否需要 case/example。

缺少题目或核心知识点时，直接指出缺少什么，并请用户补充。缺少字数或格式时可以继续先给通用大纲，但要标注“【待确认】”。

### 第 1 步：首轮输出给用户的大纲

首轮只输出中文为主、保留 English terms 的作答大纲，不输出完整英文答案。格式如下：

```markdown
# 问答题作答大纲（第 1 版）

## 1. 题目在问什么

- 用 1 至 3 条 bullet 拆解题目要求。
- 标出关键词，例如 `stakeholder`、`scope`、`risk`、`requirement`。

## 2. 可用知识点

- 只列课程材料支持的概念。
- 每个概念后说明它能服务答案的哪一部分。

## 3. 建议答案结构

### Paragraph 1 / Point 1：主题句

- 要回答的核心意思。
- 可使用的知识点。
- 可以放的例子或 case detail。
- 注意不要写偏的地方。

### Paragraph 2 / Point 2：主题句

- 按题目需要继续拆分。

## 4. 你可以先补充的想法

- 请用户补充自己的观点、例子、课程中老师强调的内容，或想采用的立场。
- 明确告诉用户：接下来会检查逻辑、遗漏、跑题和术语使用。
```

### 第 2 步：收到用户想法后的一轮诊断与修订

用户给出自己的想法后，不要直接出英文稿。先做诊断，再给核实后的中文修订大纲：

```markdown
## 你的想法里已经可用的部分

- 点出能保留、能加强答案的内容。

## 需要调整的问题

- `问题类型`：说明问题，例如跑题、概念混淆、证据不足、结构重复、英文术语不准、没有回应题目动词。

## 核实后的中文大纲

- 在原结构上修改，不要完全推翻，除非用户思路与题目明显不匹配。

## 请你确认

- 请用户确认这版中文大纲是否可以。
- 明确说明：用户同意后，再输出英文版本。
```

### 第 3 步：用户同意后输出英文版本

用户确认中文大纲可以后，只输出英文版本，除非用户要求同时重贴中文：

1. 英文版本应忠实对应已确认的中文大纲。
2. 不新增中文大纲没有的论点、例子或 case detail。

格式如下：

```markdown
# English Outline

## 1. Introduction / Opening

- English topic sentence or outline point.

## 2. Body Point 1

- Key concept.
- Supporting explanation.
- Link back to the question.
```

具体规则：

1. 全程严格依据用户提供的题目和课程知识点；不能擅自加入外部知识、虚构例子、虚构引用或未给出的 case detail。
2. 先写大纲，不直接写完整 essay，除非用户明确要求完整答案。
3. 默认一轮过：首轮大纲之后等用户想法；收到用户想法后指出问题并给核实后的中文大纲；用户同意后再输出英文版本。
4. 如果用户要求跳过协作诊断，可以直接输出英文版本，但要说明这是跳过诊断的版本。
5. 诊断用户想法时要具体指出问题位置和原因，语气直接但不打击；优先帮助用户把已有想法改好。
6. 注意题目动词：`explain` 要解释原因，`compare` 要并列对比，`discuss` 要给多面分析，`evaluate` 要判断优劣或重要性，`justify` 要说明为什么成立。
7. 保留关键 English terms，例如 `stakeholder`、`requirement`、`scope creep`、`risk`、`deliverable`、`business value`，并在中文大纲中自然嵌入。
8. 核实后的中文大纲要优先保留用户原来的想法、关键词和表达顺序；只做必要的压缩、纠偏和术语修正，避免改成完全陌生的标准答案。
9. 用户要求“更简洁”时，每个 point 默认控制在 1 至 2 句，保留最能扣题的课程术语。
10. 英文版本应忠实对应用户已同意的中文大纲，不新增中文大纲没有的论点。
11. 如果材料不足以判断答案方向，使用 `【待确认】` 标注，不把不确定内容写成确定结论。

## 一般文字说明型输出格式

默认使用“核心版学习笔记输出格式”。只有当用户明确要求“简短说明 / 摘要 / 三段式”或材料非常短时，才使用以下三部分：

```markdown
# 原章节编号与标题

## 1. 这段在说什么

用 2 至 4 句话概括核心意思。

## 2. 重点拆解

- 按原文顺序解释重点、意义和注意事项。

## 3. 小结

- 用 3 至 6 条短句总结。
```

材料带有课程编号时，保留父级与子级编号，不把不同父级章节混在一起。

## 通用排版与术语

1. 标题与正文之间空一行。
2. 每个章节、小节和 Part 之间空一行；不要连续堆叠标题和正文，也不要制造多行空白。
3. 中文为主，保留关键英语术语，例如 `class`、`object`、`compile`、`execute`。
4. 术语第一次出现时可写成“信息系统（Information Systems）”或 `Scope Creep（范围蔓延）`，之后保持统一。
5. 使用标准 Markdown 标题和 `-` 项目符号，不复制 Word 式制表符或特殊圆点层级。
6. 表达简洁，不重复解释，不自行补充材料没有讲到的延伸知识。

## FIT1051 Java Coding Conventions

用户要求检查、解释或生成 FIT1051 Java code 时，遵循以下课程约定：

1. `class` names 必须以 uppercase letter 开头，使用 mixed case / UpperCamelCase，例如 `HelloWorld`、`WelcomeUser`。
2. `method` names 和 `variable` names 必须以 lowercase letter 开头，后续单词首字母 uppercase，即 lowerCamelCase，例如 `getUserName`、`loopSize`。
3. Methods 之间 exactly one empty line。
4. Methods 按 alphabetical order 组织。
5. 如果 quiz 问“which is not a convention”，而选项包含以上规则，则应判断这些规则都是本课程 convention。

## 图片缺失提醒

1. 只在图片明显影响理解、quiz 答题或操作定位时提醒。
2. 使用 `【图片提醒】`，说明缺的是哪类图以及它可能帮助理解什么。
3. 不要每张图都提醒，不要猜测图片具体内容。

## 简洁要求

- 一条命令配一行解释。
- 不重复解释相同概念。
- 不添加无关背景、最佳实践、额外验证或延伸练习。
- 用户询问原理时再简短解释原理。
- 用户贴出终端输出时，只说明与当前教程步骤有关的成功、失败和下一步。
- 视频、slides 和文字说明材料要“详细但突出重点”，避免逐句机械翻译或平铺成长文。

## 缺失信息

如果原教程缺少继续操作必需的信息：

1. 明确写出缺少的内容。
2. 使用 `<教程指定路径>` 等占位符，或请用户提供缺失章节。
3. 替代方案必须说明“这不是教程原文”，并先取得用户同意。

## 回答前检查

- 是否正确区分教程、多段视频、slides 和一般文字说明？
- 是否从正确位置开始？
- 是否跳过已完成内容？
- 是否保持原教程顺序和名称？
- 是否保留关键英语术语？
- 多段视频是否保持一个视频一个 Part？
- Slides 是否按主题变化分节，而不是逐页翻译或整套平铺？
- 题目与答案是否提炼为不重复的核心知识点，而不是逐题复述？
- 每个题目知识点是否尽量一行讲清，并使用准确的中英文术语？
- 问答题协作写作是否先给中文大纲、等待用户想法、一轮诊断并给核实后的中文大纲，用户同意后再输出英文版本？
- 问答题大纲是否严格依据题目和课程知识点，没有直接代写完整 essay 或加入外部材料？
- 核实后的问答题大纲是否尽量保留用户原想法、关键词和表达顺序，并按用户要求保持简洁？
- 标题、Part 和章节之间是否留有一个空行？
- 是否排除了界面残留和当前材料以外的对话内容？
- 是否只在必要时添加克制的 `【图片提醒】`？
- 是否做到教程型一条命令只配一行解释？
- 是否加入了未经同意的路径、端口、软件或步骤？
