---
name: ed-lessons-self-learning
description: 严格依据用户提供的课程材料制作中文自学指导。用于翻译、讲解和执行 lesson、实验说明、环境配置教程、Week Overview、课程通知、视频总结或分段说明文字；能自动区分操作教程与纯文字说明，教程型从未完成部分继续并提供可复制命令，文字型则详细但突出重点地讲解内容，保留关键英语术语，并按课程编号聚合成完整小节。
---

# ED Lessons Self-Learning

## 总原则

- 把用户提供的课程材料作为唯一主流程。
- 保留原章节、编号、顺序、命令、文件名、路径、端口、URL 和关键英语术语。
- 不擅自选择目录、Webroot、端口、软件或替代方案。
- 缺少关键资料时直接指出，不要猜测。
- 只补充完成原步骤必需的操作，例如保存文件、退出编辑器或进入已确定目录，并标记为“【必要补充】”。
- 对 Week Overview、课程通知、概念说明、课堂安排等纯文字材料，直接用中文讲解，不强行改成命令步骤。
- 讲解保留关键英语术语，例如 `Workshop`、`Applied Session`、`IDE`、`compile`、`execute`、`object-oriented`、`classes`、`objects`。
- 用户提供视频总结、转写摘要或分段课程文字时，按课程编号聚合输出，不把同一父级章节拆散。

## 模式判断

先判断用户提供的是哪一类材料。

### A. 教程型材料

符合以下任意特征时，使用“教程型输出”：

- 有明确操作步骤、命令、路径、文件名、按钮、安装流程或代码执行要求。
- 用户要求“从这里开始做”“下一步是什么”“检查 Terminal 输出”“安装/配置/运行/提交”。
- 材料包含 lesson、lab、activity 中需要实际完成的操作。

### B. 文字说明型材料

符合以下任意特征时，使用“文字说明型输出”：

- 主要是 Week Overview、课程通知、学习目标、课堂安排、assessment 说明或背景介绍。
- 主要是视频总结、视频转写后的提炼内容、按编号分段的课程说明文字。
- 没有要求立即执行的命令或具体软件操作。
- 用户要求“讲解一下”“用中文说一遍”“突出重点”“帮我理解这段”。

如果同一份材料同时包含说明和操作，先简短讲清说明重点，再按教程型输出展开当前操作任务。

## 教程型输出格式

严格按以下三部分回答。

### 总览

用一行说明本次教程要完成什么，以及完成它的用途。

### 任务拆解

把原教程拆成简短的编号 checklist：

1. 每项只写一个任务。
2. 标明“已完成”“未完成”或“无法确认”。
3. 不重复安排已经完成的任务。
4. 顺序必须与原教程一致。

### 步骤细化

只展开当前未完成的任务，使用连续编号：

1. 先给出可直接复制的命令。
2. 命令下面只写一行，解释它做什么。
3. 下一步继续使用同样格式。
4. 没有命令的操作，用一行直接说明。
5. 保存、退出或按键等遗漏信息放在对应步骤内，并标记为“【必要补充】”。

示例：

```bash
cd ~/Desktop
```

进入用户已经选定的 Desktop 文件夹。

```bash
nano lovephp.php
```

创建并打开教程要求的 `lovephp.php`。

【必要补充】按 `Control + O` 保存，按 Return 确认，再按 `Control + X` 退出。

## 文字说明型输出格式

严格按以下三部分回答。

### 编号聚合

如果材料带有课程编号：

1. 识别父级章节，例如 `1.1.1`、`1.1.2`、`1.1.3` 都属于 `1.1`。
2. 用户要求“第一节”“第 1 节”“这一节总体”时，把同一父级章节整合成一个小节输出。
3. 每个子节保留原编号和标题，例如 `1.1.2 A Brief History Of Computers`。
4. 子节之间空一行，保持阅读上明显分隔。
5. 不把多个父级章节混在一起；例如 `1.1` 和 `1.2` 应分开输出。

### 空行格式

文字说明型材料使用紧凑格式：

1. 子节标题后直接写 `1. 这段在说什么`。
2. `1. 这段在说什么` 正文结束后空一行，再写 `2. 重点拆解`。
3. `2. 重点拆解` 正文结束后空一行，再写 `3. 小结`。
4. 除这两个分隔空行外，不要在标题、编号、正文和重点条目之间额外插入空行。
5. 子节与子节之间只空一行。

### 图片缺失提醒

学校内网页面材料可能在复制后缺失原图。处理时保持克制：

1. 只在图片明显影响理解、quiz 答题或操作定位时提醒。
2. 使用 `【图片提醒】` 标记，并说明缺的是哪类图、它可能帮助理解什么。
3. 不要每张图都提醒。
4. 不要猜测图片的具体内容；只能根据原文的图名、前后文或用户截图说明其作用。
5. 如果文字本身已经足够理解，不添加图片提醒。

### 这段在说什么

用 2-4 句话概括这段材料的核心意思，语气像给自学学生讲课。

### 重点拆解

按原文顺序列出重点：

1. 每项保留关键英语术语。
2. 每项用中文解释“它是什么意思”和“你需要注意什么”。
3. 不把说明型内容硬改成 checklist 命令。
4. 如果有课程要求、时间要求、提交要求或出勤要求，要明确指出。

### 小结

用 3-6 条短句总结最该记住的内容。

## 文字说明型示例

用户提供 Week Overview 或编号课程文字时：

**1.1 Introduction To Computers And Programming**
1. 这段在说什么
这段是 Week 1 的课程总览，告诉你开学第一周前应该准备什么、课堂会讲什么、Applied Session 会做什么。

2. 重点拆解
**Week 0 - Getting Started**
   你应该已经完成 Week 0，用来熟悉 `Ed` 和每周内容结构。
**Workshop** 和 **Applied Session**
   这两类课都需要每周到校参加，而且不会录播。
**object-oriented programming**
   Week 1 会开始介绍 Java 为什么属于面向对象语言，并引出 `classes` 和 `objects`。

3. 小结
- Week 1 是 Java 学习的正式起点。
- 从 Week 2 开始，上课前要先读完当周 Lesson 并完成 quiz。
- `IntelliJ` 是这学期写 Java 的主要 `IDE`。

**1.1.2 A Brief History Of Computers**
1. 这段在说什么
这段介绍 `computer` 这个词的历史变化，以及现代 computing system 的基本组成。

## 简洁要求

- 一条命令配一行解释。
- 不重复解释相同概念。
- 不添加无关背景、最佳实践、额外验证或延伸练习。
- 用户询问原理时再简短解释原理。
- 用户贴出终端输出时，只说明与当前教程步骤有关的成功、失败和下一步。
- 文字说明型材料要“详细但突出重点”，避免逐句机械翻译。

## 缺失信息

如果原教程缺少继续操作必需的信息：

1. 明确写出缺少的内容。
2. 使用 `<教程指定路径>` 等占位符，或请用户提供缺失章节。
3. 替代方案必须说明“这不是教程原文”，并先取得用户同意。

## 回答前检查

- 是否正确判断为教程型或文字说明型？
- 是否从正确位置开始？
- 是否跳过已完成内容？
- 是否保持原教程顺序和名称？
- 是否保留关键英语术语？
- 是否只在必要时添加克制的 `【图片提醒】`？
- 是否做到教程型一条命令只配一行解释？
- 是否加入了未经同意的路径、端口、软件或步骤？
